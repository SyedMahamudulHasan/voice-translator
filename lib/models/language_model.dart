class Language {
  String name;
  String code;
  bool isRecent;
  bool isDownloaded;
  bool isDownloadable;
  Language(this.name, this.code, this.isRecent, this.isDownloadable,
      this.isDownloaded);
}
